# Fingerprintig Web Server:

## 1. Banner Grabbing:
### Use tools like `zemap, netcraft, nmap, nikto, curl, nc`.
### Send malformed requests and observe the different erros, according to the services(Nginx, Apache, lighttpd).
